# Core dependencies (always needed)
click>=8.1,<9
numpy
opencv-python
Pillow
pyautogui
pywin32; sys_platform == "win32"

# LLM dependencies (optional, only for built-in OpenRouter mode)
# Install with: pip install weclaw[llm]
# Not needed in stepwise mode (agent handles LLM calls)
litellm
openai

# macOS dependencies (only on macOS)
# Install with: pip install weclaw[macos]
pyobjc-core
pyobjc-framework-ApplicationServices
pyobjc-framework-Cocoa
pyobjc-framework-Quartz
pyobjc-framework-Vision
