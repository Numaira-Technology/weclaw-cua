# Core dependencies (always needed)
click>=8.1,<9
numpy
opencv-python
Pillow
screenshot-stitcher
pyautogui
pywin32; sys_platform == "win32"

# LLM dependencies (optional, only for built-in LLM mode)
# Capture still needs platform OCR extras: RapidOCR on Windows, HunyuanOCR on macOS.
litellm
openai

# Windows sidebar OCR: RapidOCR on ONNX Runtime.
rapidocr-onnxruntime>=1.3; sys_platform == "win32"

# HunyuanOCR (shared/ocr_hunyuan.py; platform_mac). Omit on Linux.
torch>=2.7.1; sys_platform == "darwin"
torchvision; sys_platform == "darwin"
huggingface_hub; sys_platform == "darwin"
accelerate; sys_platform == "darwin"
transformers @ git+https://github.com/huggingface/transformers@82a06db03535c49aa987719ed0746a76093b1ec4; sys_platform == "darwin"

# macOS only (WeChat window, Vision OCR helpers)
# Install with: pip install "weclaw-cua[macos]"
pyobjc-core; sys_platform == "darwin"
pyobjc-framework-ApplicationServices; sys_platform == "darwin"
pyobjc-framework-Cocoa; sys_platform == "darwin"
pyobjc-framework-Quartz; sys_platform == "darwin"
pyobjc-framework-Vision; sys_platform == "darwin"
