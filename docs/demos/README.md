# Demo Assets

Place video and GIF demos here for the main README. Once the repo is public, update the image paths in `README.md` to point to these files.

## Suggested assets

| File | Description |
|------|-------------|
| `control-panel-workflow.gif` | Control panel in action: Start Server → Start Workflow → Classify → Filter → Read → Extract → Plan → Remove |
| `openclaw-skill.gif` | OpenClaw skill invocation: "Remove spammers from my WeChat groups" |

Recommended: Record at 1280×720 or 1920×1080, keep under 5MB for GIF, or use MP4 for video (GitHub supports both).
