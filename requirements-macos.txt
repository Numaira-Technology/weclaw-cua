# Same dependency set as:
#   pip install "weclaw-cua[macos,llm,hunyuan-ocr]"
# From this repository (macOS, editable install):
#   pip install -e ".[macos,llm,hunyuan-ocr]"
#
-e .[macos,llm,hunyuan-ocr]
